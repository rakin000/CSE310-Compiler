.MODEL SMALL
.STACK 400H
.DATA
.CODE
main PROC
	MOV AX, @DATA
	MOV DS, AX; Line 1: load data to DS
	MOV BP, SP
; Line 1: save SP
	ADD SP, -6
	MOV CX, 1
	PUSH CX
	MOV CX, 2
	PUSH CX
	MOV CX, 3
	PUSH CX
	POP AX
	POP BX
	ADD BX, AX
	PUSH BX
	POP CX
	POP AX
	MUL CX
	PUSH AX
	MOV CX, 3
	POP AX
	DIV CX
	PUSH DX
	POP AX
	MOV [BP+-2], AX
	PUSH AX
	POP AX
	MOV CX, 1
	PUSH CX
	MOV CX, 5
	PUSH CX
	POP BX
	POP AX
	CMP AX,BX
	JL L0
	PUSH 0
	JMP L1
	L0:
	PUSH 1
	L1:
	POP AX
	MOV [BP+-4], AX
	PUSH AX
	POP AX
	MOV CX, 0
	PUSH CX
	POP BX
	SHL BX, 2
	NEG BX
	ADD BX, -6
	ADD BX, BP
	MOV CX, 2
	PUSH CX
	POP AX
	MOV [BX], AX
	PUSH AX
	POP AX
	MOV CX,[BP+-2]
	PUSH CX
	MOV CX,[BP+-4]
	PUSH CX
	POP BX
	POP AX
	CMP AX, 0
	JE L2
	CMP BX, 0
	JE L2
	PUSH 1
	JMP L3
	L2:
	PUSH 0
	L3:
	MOV CX, 0
	PUSH CX
	POP BX
	SHL BX, 2
	NEG BX
	ADD BX, -6
	ADD BX, BP
	PUSH CX
	POP AX
	MOV CX, 1
	PUSH CX
	POP BX
	SHL BX, 2
	NEG BX
	ADD BX, -6
	ADD BX, BP
	MOV CX, 0
	PUSH CX
	POP BX
	SHL BX, 2
	NEG BX
	ADD BX, -6
	ADD BX, BP
	MOV CX,[BX]
	PUSH CX
	POP AX
	MOV [BX], AX
	PUSH AX
	POP AX
	MOV AH, 4CH
	INT 21H
main ENDP
